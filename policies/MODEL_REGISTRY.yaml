# Model Registry — USER-AUTHORITATIVE model configuration.
#
# HARD INVARIANT
# --------------
# Registry membership and `enabled` status are human-authoritative
# configuration, not an AI-generated capability verdict.
#
# This file expresses the operator's decision: "these are the provider/model
# candidates I allow this workflow to use, and the stage / role / reasoning I
# want them used under." An AI operating this pack MUST NOT cancel a candidate
# because of its own benchmark opinion, its own read of `evidence_status`,
# whether 3-5 smoke cases were run, or a preference for a different model.
#
# If the human put a model here and set `enabled: true` (or omitted `enabled`),
# it is ROUTING ELIGIBLE. The AI may refuse it only on genuine execution
# impossibility (provider CLI absent, model id unresolvable, reasoning control
# unsupported, auth invalid, permission ceiling incompatible, runtime cannot
# start, reviewer disjointness cannot be met) -> a real CONFIG_INVALID /
# ROUTING_UNAVAILABLE / PERMISSION_BLOCKED outcome.
#
# Among enabled candidates that pass hard execution eligibility, the AI's job
# is to pick the best fit by stage / role / disjointness / permission and then
# by quota / resource state. It does not re-rank models by quality.
#
# ADOPTERS READ THIS FIRST
# ------------------------
# The mapping shipped here reflects the AUTHORING HOST and the CLI state
# observed there at the recorded verification date. It is NOT a guarantee that
# these providers, models or plans are available to you. Before your first
# dispatch, re-resolve every candidate against your OWN installed CLIs and
# subscription plans, and edit this file to match what YOU have enabled. This
# is still user-authoritative configuration - just yours, not the author's.
#
# --------------------------------------------------------------------------
# FIELD SEMANTICS
#
#   enabled: true|false   HUMAN-authoritative. false = do not route here.
#                         Missing -> treated as true (backward compatible).
#   status: stable|experimental
#                         INFORMATIONAL evidence metadata ONLY. As of 0.6 it
#                         does NOT gate execution eligibility. `experimental`
#                         MUST NOT block a model the user has enabled.
#   evidence_status, confidence, evidence_scope, source, verified_at
#                         INFORMATIONAL. Help the human decide future registry
#                         changes. Never an AI routing gate.
#   stage, reasoning, capability_tier, model_family, candidate order
#                         HUMAN-authoritative configuration.
#
# `allow_experimental` in a contract is retained for backward compatibility and
# has NO routing effect on an enabled registry candidate (see
# MODEL_ROUTING_POLICY.md). Reasoning-exact dispatch, reviewer disjointness,
# the three-stage architecture, the flagship guard and the hierarchical quota
# windows are UNCHANGED.
#
# Stable workflow rules live in the Markdown policies and do NOT change when a
# model changes. Only this file changes when mapping changes.
#
# --------------------------------------------------------------------------
# 0.6 (2026-09-02) REGISTRY AUTHORITY CORRECTION
#
# Registry authority changed from evidence-gated AI eligibility to
# human-authoritative configuration. Every candidate now carries
# `enabled: true`. The antigravity / AUTO_GEMINI entries are ENABLED and
# routable for autonomous quota routing despite `status: experimental` /
# incomplete qualification (E6): that is informational, not a gate. Terra is
# enabled despite zero local smoke cases. See
# policies/registry-decisions/2026-09-02-user-authoritative-registry.md.
#
# Prior: 0.5 three-stage routing
# (policies/registry-decisions/2026-09-02-three-stage-routing.md);
# 0.4 implementer-tier rebalance
# (policies/registry-decisions/2026-09-02-rebalance-implementer-tiers.md).
# --------------------------------------------------------------------------

version: "0.6"
verified_at: "2026-09-02"

capability_tier_order: [CHEAP, DEFAULT, STRONG, DEEP]

# Capability stage: a backward-compatible band above capability_tier.
# Each tier belongs to exactly one stage (a partition).
stages:
  STAGE_1_DEFAULT:
    tiers: [CHEAP, DEFAULT]
    admission: default
    admission_note: >-
      The default for the majority of tasks. Used unless there is concrete
      evidence Stage 1 is insufficient. Risk, production-relatedness, security
      sensitivity and test volume do NOT by themselves raise the stage.
  STAGE_2_ADVANCED:
    tiers: [STRONG]
    admission: advanced_signal
    admission_note: >-
      Requires at least one advanced signal: high complexity, high ambiguity,
      structural change intensity, architecture reasoning, security/RLS
      semantic reasoning, high semantic coupling, adversarial verification, or
      a failed Stage 1 attempt. Risk alone is insufficient. This is the normal
      escalation tier.
  STAGE_3_FLAGSHIP:
    tiers: [DEEP]
    admission: exceptional_evidence
    admission_note: >-
      Rare. Requires exceptional evidence: Stage 2 failed to converge,
      unresolved disagreement between qualified reviewers, a major irreversible
      architectural decision with material ambiguity, adversarial security
      analysis, exceptional multi-stage agentic execution, an exceptional
      production incident, or explicit human authorization. Sol and Opus are
      flagship escalation resources, not routine workers.

# Dynamic display names must be resolved at use time, never frozen from one
# machine. See references/OFFICIAL_COMMANDS.md for the verified invocation.
resolvers:
  antigravity_models:
    command: "agy models"
    resolves: ["AUTO_GEMINI"]
    verified_at: "2026-09-02"
    note: >-
      AUTO_GEMINI is a placeholder. Resolve it against the live model list
      before dispatch. Do not hard-code a display name observed on one host.
      Verified 2026-09-02: the command returns a live list that also contains
      NON-Gemini families, so provider does not imply model_family. A
      resolution outside the candidate's declared model_family is
      CONFIG_INVALID and must not be dispatched - this is what keeps
      independent-review disjointness honest.
      Effort is encoded in the id: `reasoning: low` resolves to the
      `gemini-3.7-flash-low` family entry, `reasoning: high` to
      `gemini-3.7-flash-high` (observed 2026-09-02). `agy --effort low|medium|high`
      is the session-level equivalent. Resolve against the live list at
      dispatch time; this is a hint, not a frozen string.
      Model eligibility is evaluated PER EXECUTION TASK: the headless
      permission limitation recorded in E6 constrains specific task shapes,
      it is not global ineligibility.

capability_slots:

  ROUTER:
    role: ROUTER
    stage: STAGE_1_DEFAULT
    minimum_tier: DEFAULT
    max_repair_attempts: 2
    candidates:
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-luna
        model_family: gpt-5.6
        reasoning: max
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: bounded_repo_implementation
        confidence: medium
        source: "local CLI global state, codex-cli 0.151.0; reasoning pinned max per human decision"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-enabled Stage 1 quota-relief alternative; agy resolver verified 2026-09-02; E6 records an incomplete headless-mode qualification (informational, not a gate)"
        verified_at: null
        expires_at: null

  CHEAP_GENERALIST:
    role: IMPLEMENTATION
    stage: STAGE_1_DEFAULT
    minimum_tier: CHEAP
    max_repair_attempts: 2
    candidates:
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-luna
        model_family: gpt-5.6
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: bounded_repo_implementation
        confidence: medium
        source: "local CLI global state, codex-cli 0.151.0"
        verified_at: "2026-09-01"
        expires_at: "2026-12-01"
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-enabled quota-relief alternative; agy resolver verified 2026-09-02"
        verified_at: null
        expires_at: null

  DEFAULT_IMPLEMENTER:
    role: IMPLEMENTATION
    stage: STAGE_1_DEFAULT
    minimum_tier: DEFAULT
    max_repair_attempts: 2
    candidates:
      # STAGE 1 workhorse. Luna and Gemini Flash low are both user-enabled
      # Stage 1 candidates; if both resolve at runtime, both are eligible and
      # quota / resource routing decides. The AI does not judge which is
      # "better".
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-luna
        model_family: gpt-5.6
        reasoning: max
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: bounded_repo_implementation
        confidence: medium
        source: "local smoke case T2S1 2026-09-01 (at medium effort); reasoning pinned max per human decision"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-enabled Stage 1 quota-balancing alternative; agy resolver verified 2026-09-02; E6 qualification incomplete (informational)"
        verified_at: null
        expires_at: null

  STRONG_IMPLEMENTER:
    role: IMPLEMENTATION
    stage: STAGE_2_ADVANCED
    minimum_tier: STRONG
    max_repair_attempts: 2
    candidates:
      # STAGE 2 advanced. Terra and Sonnet 5 are peers; the resource overlay
      # chooses between them by long-horizon quota health. Gemini high is the
      # quota-pressure relief candidate. All three are user-enabled.
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-terra
        model_family: gpt-5.6
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: structural_implementation
        confidence: low
        source: "user-approved Stage 2 candidate; zero local smoke cases (informational limitation, not a gate); rollback condition in the 0.5 decision note"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: claude
        resource_state_key: claude
        model: sonnet
        model_family: claude-sonnet
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: architecture_and_contract_reasoning
        confidence: medium
        source: "local CLI, Claude Code 2.1.258; --effort high supported"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-enabled Stage 2 quota-pressure relief candidate; agy resolver verified 2026-09-02; E6 qualification incomplete (informational)"
        verified_at: null
        expires_at: null

  DEEP_REASONER:
    role: ROUTER
    stage: STAGE_2_ADVANCED
    minimum_tier: STRONG
    max_repair_attempts: 2
    candidates:
      # STAGE 2 advanced reasoning: architecture reasoning, contract ambiguity,
      # security/RLS semantics, hard debugging, high-semantic-coupling review.
      # Deliberately Stage 2, not Stage 3. The irreversible-and-ambiguous
      # subset routes to ESCALATION_MODEL via the slot decision table.
      - provider: claude
        resource_state_key: claude
        model: sonnet
        model_family: claude-sonnet
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: architecture_and_contract_reasoning
        confidence: medium
        source: "local CLI, Claude Code 2.1.258; --effort high"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-terra
        model_family: gpt-5.6
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: structural_implementation
        confidence: low
        source: "user-approved Stage 2 candidate; zero local smoke cases (informational)"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-enabled Stage 2 quota-pressure relief candidate"
        verified_at: null
        expires_at: null

  LONG_CONTEXT_DISCOVERY:
    role: LONG_CONTEXT_DISCOVERY
    stage: STAGE_1_DEFAULT
    minimum_tier: DEFAULT
    max_repair_attempts: 2
    candidates:
      # Broad repository inspection / cross-repo inventory / schema sweeps is
      # Stage 1 discovery work. Gemini Flash is the user-preferred quota-relief
      # choice on long-context evidence; Sonnet is the disjoint fallback.
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-preferred discovery candidate; external long-context evidence E4; agy resolver verified 2026-09-02; E6 discovery smoke incomplete (informational)"
        verified_at: null
        expires_at: null
      - provider: claude
        resource_state_key: claude
        model: sonnet
        model_family: claude-sonnet
        reasoning: provider_default
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: architecture_and_contract_reasoning
        confidence: medium
        source: "local CLI, Claude Code 2.1.258; disjoint discovery fallback"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"

  INDEPENDENT_REVIEWER:
    role: INDEPENDENT_REVIEWER
    stage: STAGE_1_DEFAULT
    minimum_tier: DEFAULT
    max_repair_attempts: 1
    candidates:
      # Reviewer capability matches task need, not default to flagship. The
      # selection algorithm removes any candidate sharing the implementer's
      # provider OR model family before resource optimization - this is a hard
      # filter that quota and evidence_status cannot override. The list spans
      # three providers so a disjoint reviewer exists for any single
      # implementer. Sol/Opus appear nowhere here.
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-enabled reviewer candidate; agy resolver verified 2026-09-02; E6 read-only reviewer smoke incomplete (informational, evaluated per task)"
        verified_at: null
        expires_at: null
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-luna
        model_family: gpt-5.6
        reasoning: medium
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: bounded_repo_implementation
        confidence: low
        source: "local smoke case T2S1 2026-09-01; Stage 1 reviewer where disjoint"
        verified_at: "2026-09-01"
        expires_at: "2026-12-01"
      - provider: claude
        resource_state_key: claude
        model: sonnet
        model_family: claude-sonnet
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: independent_review
        confidence: medium
        source: "local CLI, Claude Code 2.1.258; Stage 2 review where deeper reasoning is justified and disjoint"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-terra
        model_family: gpt-5.6
        reasoning: high
        capability_tier: STRONG
        stage: STAGE_2_ADVANCED
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: structural_implementation
        confidence: low
        source: "Stage 2 structural review where disjoint from a Claude implementer; zero local smoke cases (informational)"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"

  REGRESSION_HUNTER:
    role: REGRESSION_HUNTER
    stage: STAGE_1_DEFAULT
    minimum_tier: DEFAULT
    max_repair_attempts: 2
    candidates:
      # Stage 1 by default. Gemini Flash preferred for quota relief; Luna is
      # the disjoint fallback. Both user-enabled.
      - provider: antigravity
        resource_state_key: antigravity.gemini
        model: AUTO_GEMINI
        model_family: gemini
        reasoning: low
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: experimental
        evidence_status: provisional
        evidence_scope: long_context_reasoning
        confidence: low
        source: "user-preferred regression-hunter candidate; agy resolver verified 2026-09-02"
        verified_at: null
        expires_at: null
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-luna
        model_family: gpt-5.6
        reasoning: medium
        capability_tier: DEFAULT
        stage: STAGE_1_DEFAULT
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: bounded_repo_implementation
        confidence: low
        source: "local smoke case T2S1 2026-09-01"
        verified_at: "2026-09-01"
        expires_at: "2026-12-01"

  ESCALATION_MODEL:
    role: ESCALATION
    stage: STAGE_3_FLAGSHIP
    minimum_tier: DEEP
    max_repair_attempts: 1
    candidates:
      # STAGE 3 flagship. Rare. Sol and Opus 5 are peers; task-role preference
      # (terminal-heavy/agentic -> Sol, architecture/reasoning/review -> Opus)
      # is a tie-break AFTER eligibility/disjointness/resource. Both default to
      # `medium`; neither is ever defaulted to max. A Stage 3 selection MUST
      # record a flagship admission reason (see MODEL_ROUTING_POLICY.md).
      - provider: codex
        resource_state_key: codex
        model: gpt-5.6-sol
        model_family: gpt-5.6
        reasoning: medium
        capability_tier: DEEP
        stage: STAGE_3_FLAGSHIP
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: structural_implementation
        confidence: medium
        source: "user-approved flagship candidate; external evidence E3 + E7 (informational); default reasoning medium, raise to high only on task evidence; local smoke cases pending"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
      - provider: claude
        resource_state_key: claude
        model: opus
        model_family: claude-opus
        reasoning: medium
        capability_tier: DEEP
        stage: STAGE_3_FLAGSHIP
        enabled: true
        status: stable
        evidence_status: provisional
        evidence_scope: escalation_reasoning
        confidence: medium
        source: "local CLI, Claude Code 2.1.258; default reasoning medium, raise to high only on task evidence; --effort medium|high supported"
        verified_at: "2026-09-02"
        expires_at: "2026-12-01"
